<Project>
  <PropertyGroup Condition="'$(MSBuildProjectName)' == 'ProjectTest' and '$(Configuration)' == 'Debug'">
    <RuntimeIdentifier Condition="'$(RuntimeIdentifier)' == ''">win-$(Platform)</RuntimeIdentifier>
    <RuntimeIdentifiers></RuntimeIdentifiers>
  </PropertyGroup>
</Project>
