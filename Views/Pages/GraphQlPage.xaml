<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ProjectTest.Views.Pages.GraphQlPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RequestedTheme="Light"
    Background="{ThemeResource AppCanvasBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Enabled">
        <StackPanel Margin="28" Spacing="18">
            <Border Style="{ThemeResource HeroCardBorderStyle}">
                <Grid ColumnSpacing="18" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="10">
                        <Border HorizontalAlignment="Left" Style="{ThemeResource BadgeBorderStyle}">
                            <TextBlock Style="{ThemeResource BadgeTextBlockStyle}" Text="GraphQL API" />
                        </Border>
                        <TextBlock FontFamily="Segoe UI Variable Display Semib" FontSize="32" FontWeight="SemiBold" Text="GraphQL POS API demo" />
                        <TextBlock Style="{ThemeResource PageIntroTextBlockStyle}" Text="Use GraphQL queries and mutations for products, orders, and reports." TextWrapping="WrapWholeWords" />
                    </StackPanel>
                    <Grid Grid.Column="1" ColumnSpacing="10" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" Grid.Column="0" Style="{ThemeResource AccentButtonStyle}" Command="{Binding ExecuteGraphQlCommand}" Content="Execute" />
                        <Button Grid.Row="0" Grid.Column="1" Style="{ThemeResource SubtleButtonStyle}" Command="{Binding LoadSampleGraphQlCommand}" Content="POS Sample" />
                        <Button Grid.Row="1" Grid.Column="0" Style="{ThemeResource SubtleButtonStyle}" Command="{Binding LoadSchemaCommand}" Content="Schema" />
                        <Button Grid.Row="1" Grid.Column="1" Style="{ThemeResource SubtleButtonStyle}" Command="{Binding LoadOrderDetailCommand}" Content="Order Detail" />
                    </Grid>
                </Grid>
            </Border>

            <Border Style="{ThemeResource CardBorderStyle}">
                <StackPanel Spacing="14">
                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Style="{ThemeResource SectionHeaderTextBlockStyle}" Text="Query / Mutation" />
                        <Button Grid.Column="1" Style="{ThemeResource SubtleButtonStyle}" Command="{Binding LoadMutationSampleCommand}" Content="Mutation Sample" />
                    </Grid>
                    <TextBox
                        AcceptsReturn="True"
                        FontFamily="Consolas"
                        MinHeight="260"
                        Text="{Binding GraphQlQuery, Mode=TwoWay}"
                        TextWrapping="Wrap" />
                    <TextBlock Foreground="{ThemeResource AccentBrush}" Text="{Binding StatusMessage}" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>

            <Border Style="{ThemeResource CardBorderStyle}">
                <StackPanel Spacing="14">
                    <TextBlock Style="{ThemeResource SectionHeaderTextBlockStyle}" Text="Result" />
                    <TextBox
                        AcceptsReturn="True"
                        FontFamily="Consolas"
                        IsReadOnly="True"
                        MinHeight="300"
                        Text="{Binding GraphQlResult}"
                        TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
